syntax = "proto3";
package core.swap;

import "gogoproto/gogo.proto";
import "swap/params.proto";

// this line is used by starport scaffolding # genesis/proto/import

option go_package = "github.com/Bridgeless-Project/bridgeless-core/v12/x/swap/types";

// GenesisState defines the multisig module's genesis state.
message GenesisState {
  Params params = 1 [(gogoproto.nullable) = false];

//  TODO fix it
//  repeated SwapTransaction transactions = 2 [(gogoproto.nullable) = false];
//  repeated Submissions submissions = 3 [(gogoproto.nullable) = false];
  // this line is used by starport scaffolding # genesis/proto/state
}
